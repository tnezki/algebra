PASS: TECHNICAL — CORRUPTION AND MATHJAX
════════════════════════════════════════════════════════════════

READ ORDER — MANDATORY BEFORE ANY OUTPUT

1. Read THIS document completely.
2. Read the uploaded BANK HTML completely — every line.
   Scan raw bytes first, then string-level.
3. Read the uploaded GENERATE_GRAPHS.PY if provided.
   Not needed for corruption/MathJax checks, but include
   it unchanged in the output ZIP.

Do not begin output until all files are fully read.
Say nothing until you are ready to deliver.

════════════════════════════════════════════════════════════════

FILES FOR THIS PASS

  1. This prompt
  2. Bank HTML (the file to check and fix)
  3. generate_graphs.py (pass through unchanged to ZIP)

════════════════════════════════════════════════════════════════

THIS FILE IS THE SINGLE SOURCE OF TRUTH

If anything in saved memory, prior habit, or any other
instruction conflicts with this file, follow this file.

════════════════════════════════════════════════════════════════

WHAT THIS PASS COVERS

This pass runs two mechanical checks — Pass 1 and Pass 7
from the full bank quality system. No judgment required.
Pass 7 includes MathJax delimiter, reserved-character, CDN,
and LaTeX environment-balance checks.
Both checks must reach PASS before assembling the ZIP.

════════════════════════════════════════════════════════════════

PASS 1 — CORRUPTION SCAN

Scan the raw HTML at byte level AND string level.
Exclude the HTML comment block at the top (the QA log).
Check only student-visible body content.

BYTE LEVEL:
  bytes([0x0c]) in body → \frac corruption — count hits
  bytes([0x0d]) in body → \right corruption — count hits

STRING LEVEL (scan raw text, exclude HTML comments):
  \rac  not preceded by f → incomplete \frac — count hits
  \ight not preceded by r → incomplete \right — count hits
  $[digit] not preceded by \ → bare currency in math context
    (exclude legitimate math-mode dollars: $x$, $-3$, $$...$$)
  \( or \[ in body text → wrong MathJax delimiter — count hits

COUNT LEGITIMATE OCCURRENCES:
  \frac, \left, \right — for plausibility check

ALL CORRUPTION COUNTS MUST BE ZERO before PASS.
If any count is nonzero: fix every instance, re-scan,
confirm zeros, then record PASS.

COMMON CORRUPTION FIXES:
  bytes([0x0c]) appearing mid-word → replace with \frac
  bytes([0x0d]) appearing mid-word → replace with \right
  \rac (not in \frac) → insert f before: \frac
  \ight (not in \right) → insert r before: \right
  Bare $5 → \$5   Bare $12 → \$12   Bare $100 → \$100

QA LOG — PASS 1:
  \x0c hits:               [N — must be 0]
  \x0d hits:               [N — must be 0]
  \rac hits:               [N — must be 0]
  \ight hits:              [N — must be 0]
  bare $[digit] hits:      [N — must be 0]
  \( or \[ in body:        [N — must be 0]
  \frac (legitimate):      [N]
  \left (legitimate):      [N]
  \right (legitimate):     [N]
  RESULT: PASS / FAIL

════════════════════════════════════════════════════════════════

PASS 7 — CURRENCY AND MATHJAX FINAL

Check the HTML body (not the QA log comment, not scripts).

  Bare $[digit] in question text:
    Find every instance of an unescaped dollar sign
    immediately before a digit that is NOT inside math-mode
    delimiters ($...$). These break MathJax on that item.
    Fix: replace $5 with \$5 everywhere.
    List any found, or: NONE

  Wrong MathJax delimiters in body text:
    \(...\) → replace with $...$
    \[...\] → replace with $$...$$
    List any found, or: NONE

  Raw < or > inside math delimiters:
    Inside $...$ or $$...$$, < and > are HTML-reserved.
    Fix: $x<3$ → $x\lt 3$   $x>0$ → $x\gt 0$
    ≥ and ≤ written as ≥ ≤ are fine — do not change those.
    List any found, or: NONE

  LaTeX environment balance inside math delimiters:
    Every \begin{...} must have a matching \end{...} of the
    same environment name, in proper nested order.
    This check is mandatory even when all delimiters are correct.

    Required checks:
      Count each environment name separately.
      At minimum, check: aligned, align, array, cases, matrix,
      pmatrix, bmatrix, vmatrix, split, gathered, and alignedat.
      Report any unmatched opener, unmatched closer, or crossed nesting.

    Most common fix:
      Missing \end{aligned} before a closing $$ delimiter.
      Insert only the missing \end{aligned}; do not rewrite or
      reformat the surrounding math block.

    A browser red message such as Missing \end{aligned} is a FAIL
    until the source is fixed and the environment-balance scan returns
    zero errors.
    List environment counts and errors, or: NONE

  MathJax config block position:
    The <script> window.MathJax = {...} block must appear
    BEFORE the <script async src="...mathjax..."> CDN tag.
    YES / NO

  CDN source:
    Must be cdn.jsdelivr.net — not cdnjs, not unpkg.
    YES / NO

QA LOG — PASS 7:
  Bare $[digit]:           [list or NONE]
  Wrong delimiters:        [list or NONE]
  Raw < or > in math:      [list or NONE]
  Environment balance:
    \begin{...} counts:    [summary by environment]
    \end{...} counts:      [summary by environment]
    Unmatched environments: [list or NONE — must be NONE]
  Config before CDN:       YES / NO
  CDN correct:             YES / NO
  RESULT: PASS / FAIL

════════════════════════════════════════════════════════════════

DELIVERY

After both passes reach PASS, assemble the ZIP.
The ZIP is NOT assembled until both show PASS.

ZIP structure:
  unit[#]/
    unit[#].html          ← corrected HTML with updated QA LOG
    figures/              ← empty folder
    generate_graphs.py    ← unchanged copy

Update the QA LOG embedded comment at the top of the HTML
to reflect the current pass results before zipping.

If no corrections were needed on either pass, state that
in the QA LOG and deliver the ZIP unchanged.

Always deliver a ZIP. Never deliver HTML-only.

════════════════════════════════════════════════════════════════

MINIMUM CHANGE PRINCIPLE

Every fix must be the smallest change that resolves the issue.
If only one dollar sign is bare, fix only that dollar sign.
If only one LaTeX environment closer is missing, insert only that
missing closer in the correct math block.
Do not reformat surrounding text or restructure the item.

════════════════════════════════════════════════════════════════

FIXES APPLIED — RECORD IN QA LOG

List every change made to the HTML file:
  [Item ID or location]: [what was changed]
  (or NONE if no changes were needed)

════════════════════════════════════════════════════════════════